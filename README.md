# Login-Page-Test-Automation
A robust UI automation testing framework developed using Python, Selenium WebDriver, PyTest, and the Page Object Model (POM). The framework validates multiple login scenarios with reusable components, explicit waits, logging, and HTML reporting.
